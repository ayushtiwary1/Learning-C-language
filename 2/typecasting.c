#include <stdio.h>
int main() {
    float a = 10.4;
    // if i want to change the float value to int
   int b = (int)a; // typecasting
    printf("The value after typecasting float to int is %d\n", b);

return 0;
}