   // int income , House, Car;
    // printf("Enter your annual income: ");
    // scanf("%d", &income);           
    // printf("Do you own a house? (1 for Yes / 0 for No): ");
    // scanf("%d", &House);        
    // printf("Do you own a car? (1 for Yes / 0 for No): ");
    // scanf("%d", &Car);
    // if (income > 50000 && House == 1 && Car == 1) {
    //     printf("You are eligible for a premium credit card.\n");
    // } 
    // else if (income > 30000 && ( House == 1 || Car == 1) {
    //     printf("You are eligible for a standard credit card.\n");
    // }   
    // else {
    //     printf("You are not eligible for a credit card.\n");
    // }
