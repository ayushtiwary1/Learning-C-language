#include <stdio.h>
int main() {
  int a;
  int b;
  int c;
  int cab;
  int A; 
  // both a and A are different variables due to case sensitivity
 //   int ayush is good; // invalid variable name due to spacesn
    int ayush_tiwary; // valid variable name with underscore
    return 0;

    // variable names cannot start with a digit

    
}